<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/style.css" />
    <title>Your Tasks</title>
</head>
<body class="body-index2">
    <%- include('../partials/_navbar.ejs') %>
    <div class="all-tasks-body">
       
        <div class="all-tasks-header">
    <h1>TODAY'S TASKS!</h1>

    <a class="add-task-link" href="/users/<%=user._id%>/tasks/new">Add New Task</a>
        </div>
    <ul>
        <%tasks.forEach((task)=>{%>
        <li class="task-added">
            <label class="task-label">
            <input type="checkbox" 
                   name="task-<%= task._id %>" 
                   id="task-<%= task._id %>" 
                   class="task-checkbox"

                   <% if (task.completed) { %> checked <% } %> 
                   data-task-id="<%= task._id %>" />
                <a class="task-title" href="/users/<%= user._id %>/tasks/<%= task._id %>">

                    <%= task.title %> - <%= task.date %>
               
                </a>
            </label>
        </li>
       <% })%>
    </ul>
</div>
</body>
</html>